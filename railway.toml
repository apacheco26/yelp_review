[deploy]
startCommand = "python main.py"
cronSchedule = "0 */4 * * *"
```

- `startCommand` — tells Railway what command to run when the cron fires
- `cronSchedule` — the cron schedule in cron syntax, `0 */4 * * *` means every 4 hours

Breaking down `0 */4 * * *`:
```
